<!DOCTYPE html>
<html lang="bg">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ЗАНДАН - Ръководство</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Architects+Daughter&display=swap');

        body {
            background-color: #000;
            color: #fff;
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
            line-height: 1.6;
        }

        /* НАВИГАЦИЯ - 1:1 С АРХИВА */
        nav {
            background: rgba(20, 20, 20, 0.9);
            padding: 15px;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid #333;
            text-align: center;
        }

        nav a {
            color: #aaa;
            text-decoration: none;
            margin: 0 15px;
            text-transform: uppercase;
            font-size: 0.8em;
            letter-spacing: 1px;
            transition: 0.3s;
        }

        nav a:hover, nav a.active { color: #d4af37; }

        /* ПРЕВКЛЮЧВАТЕЛ НА ЕЗИЦИ */
        .lang-switch {
            margin: 20px 0;
            text-align: center;
        }

        .lang-btn {
            background: #1a1a1a;
            color: #fff;
            border: 1px solid #444;
            padding: 8px 20px;
            cursor: pointer;
            text-transform: uppercase;
            font-size: 0.7em;
            margin: 0 5px;
            transition: 0.3s;
        }

        .lang-btn.active {
            background: #d4af37;
            color: #000;
            border-color: #d4af37;
        }

        /* СТИЛ НА СТРАНИЦИТЕ С ТЕКСТ */
        .manual-page {
            max-width: 800px;
            margin: 40px auto;
            background: #0a0a0a;
            padding: 50px;
            border: 1px solid #1a1a1a;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        h2 {
            font-family: 'Architects Daughter', cursive;
            color: #d4af37;
            border-bottom: 1px solid #d4af37;
            padding-bottom: 10px;
            margin-top: 0;
        }

        /* ГРАФИКИ (КВАДРАТЧЕТА/КАРТИ) */
        .map-grid {
            display: inline-grid;
            grid-template-columns: repeat(10, 25px); /* Може да се настройва според картата */
            gap: 2px;
            background: #222;
            padding: 5px;
            border: 1px solid #444;
            margin: 20px 0;
        }

        .cell {
            width: 25px;
            height: 25px;
            background: #000;
            border: 1px solid #111;
        }

        /* СКРИВАНЕ/ПОКАЗВАНЕ НА ЕЗИК */
        [lang="en"] { display: none; }
    </style>
</head>
<body>

    <nav>
        <a href="index.html">Начало</a>
        <a href="hero.html">Герой</a>
        <a href="manual.html" class="active">Ръководство</a>
        <a href="bestiary.html">Бестиарий</a>
        <a href="archive.html">Архив</a>
    </nav>

    <div class="lang-switch">
        <button class="lang-btn active" onclick="setLang('bg')">Български</button>
        <button class="lang-btn" onclick="setLang('en')">English</button>
    </div>

    <div id="content">
        <div class="manual-page">
            <div class="bg-text" lang="bg">
                <h2>Добре дошли в ЗАНДАН</h2>
                <p>Очаквам първите 5 страници, за да ги превърна в текст...</p>
            </div>
            <div class="en-text" lang="en">
                <h2>Welcome to ZANDAN</h2>
                <p>Waiting for the first 5 pages to convert them into text...</p>
            </div>
        </div>
    </div>

    <script>
        function setLang(lang) {
            document.querySelectorAll('[lang]').forEach(el => {
                el.style.display = 'none';
            });
            document.querySelectorAll(`[lang="${lang}"]`).forEach(el => {
                el.style.display = 'block';
            });
            
            document.querySelectorAll('.lang-btn').forEach(btn => {
                btn.classList.remove('active');
                if(btn.innerText.toLowerCase().includes(lang === 'bg' ? 'бълг' : 'eng')) {
                    btn.classList.add('active');
                }
            });
        }
    </script>
</body>
</html>
